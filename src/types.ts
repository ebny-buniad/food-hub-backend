export interface ProviderProfile {
    image: string
    restaurentName: string,
    description: string,
    address: string,
    isOpen: boolean
}

// * Categories type
export interface Categories {
    cuisine: string[]
}

